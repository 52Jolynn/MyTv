tv_station=create table tv_station(id INTEGER primary key AUTOINCREMENT, name varchar(16), owner varchar(32));
program_table=create table program_table(id INTEGER primary key AUTOINCREMENT, station int, stationName varchar(16), program varchar(512), airtime time, week int);